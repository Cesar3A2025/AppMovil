<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base theme -->
    <style name="Base.Theme.ProyectoMovil" parent="Theme.Material3.DayNight.NoActionBar" />

    <style name="Theme.ProyectoMovil" parent="Base.Theme.ProyectoMovil" />

    <style name="Theme.MyApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/md_theme_primary</item>
        <item name="colorOnPrimary">@color/md_theme_onPrimary</item>
        <item name="colorSurface">@color/md_theme_surface</item>
        <item name="colorOnSurface">@color/md_theme_onSurface</item>
        <item name="colorOutline">@color/md_theme_outline</item>

        <!-- Aplica estilo global a todos los MaterialCardView -->
        <item name="materialCardViewStyle">@style/Widget.MyApp.MaterialCardView</item>
    </style>

    <!-- ✅ Estilo personalizado para MaterialCardView -->
    <style name="Widget.MyApp.MaterialCardView" parent="Widget.MaterialComponents.CardView">
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">6dp</item>
        <item name="strokeWidth">1dp</item>
        <item name="strokeColor">?attr/colorOutline</item>
    </style>

</resources>
